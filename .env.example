# Create a client at https://www.warcraftlogs.com/api/clients/ to get these values.
# The form requires a redirect URL, but the client_credentials flow used here never
# redirects, so any value satisfies the form.

NUXT_WCL_CLIENT_ID=
NUXT_WCL_CLIENT_SECRET=
